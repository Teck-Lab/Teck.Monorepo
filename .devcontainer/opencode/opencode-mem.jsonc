{
  // opencode-mem config, seeded to ~/.config/opencode/opencode-mem.jsonc by
  // postCreate.sh. The plugin auto-installs via opencode.json's `plugin` array.

  // Storage lives UNDER the persisted opencode data volume (~/.local/share/opencode
  // is a named volume, see devcontainer.json mounts), so captured memories survive
  // container rebuilds. (Default would be ~/.opencode-mem/data — ephemeral.)
  "storagePath": "~/.local/share/opencode/opencode-mem/data",

  // On-device embedding model, downloaded from HuggingFace on first use (needs
  // network once; no API key). No external DB — SQLite + local vector index.
  "embeddingModel": "Xenova/nomic-embed-text-v1",

  // Memory web UI at http://127.0.0.1:4747 (port forwarded in devcontainer.json).
  "webServerEnabled": true,
  "webServerPort": 4747,

  // Auto-capture summarizes technical work into memory via a background AI request.
  "autoCaptureEnabled": true,
  "autoCaptureLanguage": "auto",
  "showAutoCaptureToasts": true,
  "opencodeProvider": "litellm",

  // MUST be a model whose ENTIRE LiteLLM pool supports `response_format:
  // {"type":"json_object"}` — auto-capture asks for structured JSON on every
  // request. This was deepseek-v4-flash, which failed 100% of the time: no
  // deepseek-v4 route does JSON mode (not Go, not Zen free, not even DeepSeek's
  // own first-party API — only the NVIDIA NIM route), and usage-based-routing-v2
  // prefers the rpm-less Go routes, so it never landed on the one that works.
  // Go reports the rejection as a generic 400 "Upstream request failed", which
  // reads like a provider outage rather than an unsupported parameter.
  // mimo-v2.5 is verified JSON-capable on all four of its routes (Go sub A, Go
  // sub B, Zen free, OpenRouter) and is flat-rate + fast (~1.9s), which is what
  // this high-frequency background caller needs. Re-verify JSON mode before
  // switching this to another model.
  "opencodeModel": "mimo-v2.5"
}
