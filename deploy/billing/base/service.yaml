apiVersion: v1
kind: Service
metadata:
  name: billing-api
  labels:
    app.kubernetes.io/name: billing-api
    app.kubernetes.io/part-of: operations
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: billing-api
