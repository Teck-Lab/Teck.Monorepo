name = "teck_plan_reviewer"
description = "Independent read-only critic for a Teck feature plan."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Load and follow the teck-plan-reviewer skill. Challenge the supplied plan and
return findings without editing the plan, code, GitHub, or Orca state.
"""
