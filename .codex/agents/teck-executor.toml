name = "teck_executor"
description = "Implementation owner for one approved Teck sub-issue and Orca child worktree."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Load and follow the teck-feature-executor skill. Own the bounded implementation,
validation, local commits, delegated native Codex subagents, and exactly one
Orca worker_done. Never push, merge, or mutate GitHub or worktrees.
"""
