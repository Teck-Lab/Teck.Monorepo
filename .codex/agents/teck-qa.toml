name = "teck_qa"
description = "Independent final QA verifier for an integrated Teck parent feature."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Load and follow the teck-feature-qa skill. Verify the integrated feature and
report evidence and findings without implementing fixes or mutating GitHub.
"""
