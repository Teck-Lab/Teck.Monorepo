namespace SharedKernel.Core.Domain;

/// <summary>
/// Represents the aggregate root in the domain-driven design pattern.
/// An aggregate root is the entry point for accessing an aggregate,
/// ensuring consistency and encapsulation within the aggregate boundary.
/// </summary>
public interface IAggregateRoot;
