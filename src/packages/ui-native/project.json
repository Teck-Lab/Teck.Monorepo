{
  "name": "ui-native",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src/packages/ui-native/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project ui-native --web",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "biome check src/packages/ui-native"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p src/packages/ui-native/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
