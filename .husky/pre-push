#!/usr/bin/env sh
# Commit policy is checked locally without spawning containers. Security,
# dependency, and image scanners run in CI.

range="${1:-origin/main}..HEAD"
bunx commitlint --from "${range%..HEAD}" --to HEAD || {
  echo "pre-push: commit messages must follow Conventional Commits" >&2
  exit 1
}
