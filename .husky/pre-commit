#!/usr/bin/env sh
# Pre-commit gate: fast local formatting and linting only.
# Container-backed security scans run in CI, never from Git hooks.

bunx biome check --staged --no-errors-on-unmatched || {
  echo "pre-commit: biome check failed — run 'bunx biome check --write' and restage" >&2
  exit 1
}
