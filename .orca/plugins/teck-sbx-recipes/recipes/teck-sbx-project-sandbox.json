{
  "schemaVersion": 1,
  "id": "teck-sbx-project-sandbox",
  "name": "Teck Docker Sandbox (project-shared)",
  "description": "One Docker Sandbox microVM per Orca project, with Windows lifecycle support and Teck OMP provisioning.",
  "create": "powershell.exe -NoProfile -NonInteractive -Command $ProgressPreference='SilentlyContinue';$InformationPreference='SilentlyContinue';$PSDefaultParameterValues['*:ProgressAction']='SilentlyContinue';$ErrorActionPreference='Stop';if([string]::IsNullOrWhiteSpace($env:APPDATA)){throw 'APPDATA is unavailable'};$k='teck-lab.teck-sbx-recipes';$b=[IO.Path]::GetFullPath((Join-Path $env:APPDATA 'Orca\\plugins'));$l=Get-Content -Raw (Join-Path $b 'plugins.lock.json')^|ConvertFrom-Json;$h=$l.plugins.$k.contentHash;if($h -notmatch '^^[0-9a-f]{64}$'){throw 'Installed Teck plugin hash is invalid'};$d=[IO.Path]::GetFullPath((Join-Path $b (Join-Path $k $h)));$m=Get-Content -Raw (Join-Path $d 'orca-plugin.json')^|ConvertFrom-Json;if($m.publisher -ne 'teck-lab' -or $m.id -ne 'teck-sbx-recipes'){throw 'Installed Teck plugin manifest is invalid'};$x=[IO.Path]::GetFullPath((Join-Path $d 'scripts\\lifecycle.mjs'));if(-not $x.StartsWith($d+[IO.Path]::DirectorySeparatorChar)-or -not(Test-Path -LiteralPath $x)){throw 'Installed Teck plugin lifecycle is missing'};$args=@($x,'create');^& node @args;$exitCode=$LASTEXITCODE;if($null -eq $exitCode){exit 1}else{exit $exitCode}",
  "suspend": "powershell.exe -NoProfile -NonInteractive -Command $ProgressPreference='SilentlyContinue';$InformationPreference='SilentlyContinue';$PSDefaultParameterValues['*:ProgressAction']='SilentlyContinue';$ErrorActionPreference='Stop';if([string]::IsNullOrWhiteSpace($env:APPDATA)){throw 'APPDATA is unavailable'};$k='teck-lab.teck-sbx-recipes';$b=[IO.Path]::GetFullPath((Join-Path $env:APPDATA 'Orca\\plugins'));$l=Get-Content -Raw (Join-Path $b 'plugins.lock.json')^|ConvertFrom-Json;$h=$l.plugins.$k.contentHash;if($h -notmatch '^^[0-9a-f]{64}$'){throw 'Installed Teck plugin hash is invalid'};$d=[IO.Path]::GetFullPath((Join-Path $b (Join-Path $k $h)));$m=Get-Content -Raw (Join-Path $d 'orca-plugin.json')^|ConvertFrom-Json;if($m.publisher -ne 'teck-lab' -or $m.id -ne 'teck-sbx-recipes'){throw 'Installed Teck plugin manifest is invalid'};$x=[IO.Path]::GetFullPath((Join-Path $d 'scripts\\lifecycle.mjs'));if(-not $x.StartsWith($d+[IO.Path]::DirectorySeparatorChar)-or -not(Test-Path -LiteralPath $x)){throw 'Installed Teck plugin lifecycle is missing'};$args=@($x,'suspend');^& node @args;$exitCode=$LASTEXITCODE;if($null -eq $exitCode){exit 1}else{exit $exitCode}",
  "resume": "powershell.exe -NoProfile -NonInteractive -Command $ProgressPreference='SilentlyContinue';$InformationPreference='SilentlyContinue';$PSDefaultParameterValues['*:ProgressAction']='SilentlyContinue';$ErrorActionPreference='Stop';if([string]::IsNullOrWhiteSpace($env:APPDATA)){throw 'APPDATA is unavailable'};$k='teck-lab.teck-sbx-recipes';$b=[IO.Path]::GetFullPath((Join-Path $env:APPDATA 'Orca\\plugins'));$l=Get-Content -Raw (Join-Path $b 'plugins.lock.json')^|ConvertFrom-Json;$h=$l.plugins.$k.contentHash;if($h -notmatch '^^[0-9a-f]{64}$'){throw 'Installed Teck plugin hash is invalid'};$d=[IO.Path]::GetFullPath((Join-Path $b (Join-Path $k $h)));$m=Get-Content -Raw (Join-Path $d 'orca-plugin.json')^|ConvertFrom-Json;if($m.publisher -ne 'teck-lab' -or $m.id -ne 'teck-sbx-recipes'){throw 'Installed Teck plugin manifest is invalid'};$x=[IO.Path]::GetFullPath((Join-Path $d 'scripts\\lifecycle.mjs'));if(-not $x.StartsWith($d+[IO.Path]::DirectorySeparatorChar)-or -not(Test-Path -LiteralPath $x)){throw 'Installed Teck plugin lifecycle is missing'};$args=@($x,'resume');^& node @args;$exitCode=$LASTEXITCODE;if($null -eq $exitCode){exit 1}else{exit $exitCode}",
  "destroy": "powershell.exe -NoProfile -NonInteractive -Command $ProgressPreference='SilentlyContinue';$InformationPreference='SilentlyContinue';$PSDefaultParameterValues['*:ProgressAction']='SilentlyContinue';$ErrorActionPreference='Stop';if([string]::IsNullOrWhiteSpace($env:APPDATA)){throw 'APPDATA is unavailable'};$k='teck-lab.teck-sbx-recipes';$b=[IO.Path]::GetFullPath((Join-Path $env:APPDATA 'Orca\\plugins'));$l=Get-Content -Raw (Join-Path $b 'plugins.lock.json')^|ConvertFrom-Json;$h=$l.plugins.$k.contentHash;if($h -notmatch '^^[0-9a-f]{64}$'){throw 'Installed Teck plugin hash is invalid'};$d=[IO.Path]::GetFullPath((Join-Path $b (Join-Path $k $h)));$m=Get-Content -Raw (Join-Path $d 'orca-plugin.json')^|ConvertFrom-Json;if($m.publisher -ne 'teck-lab' -or $m.id -ne 'teck-sbx-recipes'){throw 'Installed Teck plugin manifest is invalid'};$x=[IO.Path]::GetFullPath((Join-Path $d 'scripts\\lifecycle.mjs'));if(-not $x.StartsWith($d+[IO.Path]::DirectorySeparatorChar)-or -not(Test-Path -LiteralPath $x)){throw 'Installed Teck plugin lifecycle is missing'};$args=@($x,'destroy');^& node @args;$exitCode=$LASTEXITCODE;if($null -eq $exitCode){exit 1}else{exit $exitCode}"
}
